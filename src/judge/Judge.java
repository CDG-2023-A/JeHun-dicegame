package judge;

import player.Player;

import java.util.ArrayList;
import java.util.List;

public class Judge {
    public Judge [] Judge;
    public String name;
    public void Start(int round) { // 게임 시작
        for (int i = 0; i < round; i++) {
        }
    }

    // 선수 등록 방법 -> 1. 배열 2. 리스트 -> 배열 사용 X 이므로 리스트 사용.. 개념 부족
    // Judge에서 Recorder를 불러오는 방법 ?
}
